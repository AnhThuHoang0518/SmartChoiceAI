# AI Collaboration Log — SmartChoiceAI

Bằng chứng "100% AI-native" cho Vietnam Innovation Challenge 2026.

## Nộp gì vào ô "AI collaboration log"

### 1. File phiên gốc (bằng chứng chính) — thư mục session-files/
- `smartchoice-phien-chinh.jsonl` (18 MB) — transcript đầy đủ quá trình build
  SmartChoiceAI qua Claude/Cowork (1.265 lượt trao đổi nhắc tới sản phẩm).
- `smartchoice-phien-2.jsonl` (5,5 MB) — phiên tiếp nối.
Đây là file session gốc mà BTC yêu cầu ("session files"). Nén cả thư mục
session-files/ thành .zip rồi upload, hoặc up Google Drive dán link.

### 2. Codex (nếu đồng đội có dùng)
Lấy thêm thư mục `%USERPROFILE%\.codex\sessions\` trên máy người chạy Codex.

### 3. Ảnh chụp màn hình
Vài ảnh đoạn chat tiêu biểu (thiết kế kiến trúc, chấm giám khảo → vá bug...).

## Tài liệu diễn giải (đọc kèm cho giám khảo)
- `nhat-ky-cong-tac-ai.md` — tóm tắt các quyết định lớn do AI đề xuất.
- `mo-ta-giai-phap.md` — mô tả giải pháp ~1000 từ.

## Lưu ý
File .jsonl chứa dữ liệu ĐMX (do chính BTC phát cho cuộc thi) — nộp lại cho
BTC không vi phạm NDA, NHƯNG tuyệt đối không đăng công khai ra ngoài.
